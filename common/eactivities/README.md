# eactivities

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eactivities` to build the library.

## Running unit tests

Run `nx test eactivities` to execute the unit tests via [Jest](https://jestjs.io).
